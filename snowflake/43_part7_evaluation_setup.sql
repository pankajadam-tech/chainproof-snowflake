-- ChainProof Part 7: internal stage for the official Cortex Analyst evaluation YAML.
USE ROLE GRIZZLY03_LEARNER_RL;
USE WAREHOUSE GRIZZLY03_WH;
USE DATABASE CHAINPROOF;
USE SCHEMA CHAINPROOF.SEMANTIC;

CREATE OR REPLACE FILE FORMAT CHAINPROOF.SEMANTIC.PART7_YAML_FILE_FORMAT
  TYPE = CSV
  FIELD_DELIMITER = NONE
  RECORD_DELIMITER = '\n'
  SKIP_HEADER = 0
  FIELD_OPTIONALLY_ENCLOSED_BY = NONE
  ESCAPE_UNENCLOSED_FIELD = NONE;

CREATE OR REPLACE STAGE CHAINPROOF.SEMANTIC.PART7_EVALUATION_STAGE
  FILE_FORMAT = CHAINPROOF.SEMANTIC.PART7_YAML_FILE_FORMAT
  COMMENT = 'Internal stage for ChainProof Cortex Analyst evaluation configuration.';
