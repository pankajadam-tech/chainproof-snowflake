# Part 9 Runtime Evidence

## Status

PENDING — generated by `scripts/certify_part9_commit.sh` only after the real two-pass Snowflake gate and manual browser smoke pass.

## Expected evidence

The certification script records:

- repository base commit;
- operator and UTC execution time;
- Snowflake CLI version;
- real runtime log path and SHA-256;
- application URL command output;
- deterministic object and row counts;
- publication-gate result;
- actual Search/Agent/fallback capability state;
- manual browser result JSON;
- and the no-approval/no-write safety result.

No official native capability is claimed unless the corresponding Snowflake creation and smoke test passed.
